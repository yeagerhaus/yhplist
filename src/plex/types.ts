export interface PlexTrack {
	ratingKey: string;
	title: string;
	artist: string;
	album: string;
}

export interface PlexMusicSection {
	key: string;
	title: string;
}

export interface PlexPlaylist {
	ratingKey: string;
	title: string;
}
